apply plugin: 'com.android.application'
android {
    compileSdkVersion 21
    buildToolsVersion "21.1.2"
    defaultConfig {
        applicationId "com.michiganbusinessnetwork.radio"
        minSdkVersion 8
        targetSdkVersion 21
        versionName '1.3'
        versionCode 4
    }
    signingConfigs {
        release {
            storeFile file('../doc/mbn.keystore')
            storePassword 'mBn_r@d1o'
            keyAlias 'mbn_keystore'
            keyPassword 'mBn_r@d1o'
        }
    }
    buildTypes {
        release {
            signingConfig signingConfigs.release
            zipAlignEnabled true
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
    
    dependencies {
        compile "com.android.support:appcompat-v7:21.0.2"
    }
}
